#!/bin/bash

echo "========================================="
echo "实验环境信息"
echo "========================================="
echo ""
echo "【硬件信息】"
echo "-----------------------------------------"
echo "CPU 型号: $(lscpu | grep -E "型号名称|Model name" | cut -d':' -f2 | xargs)"
echo "CPU 核心数: $(nproc)"
echo "内存总量: $(free -h | awk '/^(Mem|内存)/ {print $2}')"
echo ""
echo "【软件信息】"
echo "-----------------------------------------"
echo "OS 版本: $(lsb_release -ds 2>/dev/null || cat /etc/os-release | grep PRETTY_NAME | cut -d'"' -f2)"
echo "内核版本: $(uname -r)"
echo "Node.js 版本: $(node --version 2>/dev/null || echo '未安装')"
echo "TypeScript 版本: $(npx tsc --version 2>/dev/null || echo '未安装')"
echo "========================================="
